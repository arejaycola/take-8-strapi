{
    "routes": [
        {
            "method": "GET",
            "path": "/users/me",
            "handler": "users.me",
            "config": {
                "policies": []
            }
        }
    ]
}
